<h1>Tweet Details</h1>
<strong><%= @tweet.user.username %></strong>
<div><%= @tweet.body %></div>
<div class="mt-3">
  <strong>Created At:</strong> <%= @tweet.created_at.strftime('%Y-%m-%d %H:%M:%S') %>
</div>
<%= link_to 'Edit', edit_tweet_path(@tweet), class: 'btn btn-primary' %>
<%= link_to 'Back to List', dashboard_path, class: 'btn btn-secondary' %>
